package Behavioral.Observer.WeatherJavaObservable;

/**
 * The DisplayElement interface that only has one method, display(), which will be called when the display element needs
 * to be displayed
 */
public interface DisplayElement {
    public void display();
}
